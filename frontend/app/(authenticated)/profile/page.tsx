import { AppPlaceholderPage } from "@/components/app-shell/app-placeholder-page";

export default function ProfilePage() {
  return (
    <AppPlaceholderPage
      description="Profile area placeholder."
      emptyDescription="Profile workflows are intentionally not implemented in the app shell."
      title="Profile"
    />
  );
}
