import { AppPlaceholderPage } from "@/components/app-shell/app-placeholder-page";

export default function SecretsPage() {
  return (
    <AppPlaceholderPage
      description="Secret metadata area placeholder."
      emptyDescription="Secret workflows are intentionally not implemented in the app shell."
      title="Secrets"
    />
  );
}
