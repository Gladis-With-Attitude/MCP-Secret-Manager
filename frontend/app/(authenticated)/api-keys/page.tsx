import { AppPlaceholderPage } from "@/components/app-shell/app-placeholder-page";

export default function ApiKeysPage() {
  return (
    <AppPlaceholderPage
      description="API key navigation area placeholder."
      emptyDescription="API key workflows are intentionally not implemented in the app shell."
      title="API Keys"
    />
  );
}
