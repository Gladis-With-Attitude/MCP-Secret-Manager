global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: mcp-secret-manager-rest
    metrics_path: /v1/metrics
    scheme: http
    static_configs:
      - targets:
          - backend:8000
        labels:
          service: mcp-secret-manager
          environment: development
